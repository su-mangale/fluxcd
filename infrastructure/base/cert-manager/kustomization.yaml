resources:
  - namespace.yaml
  - helmrepo.yaml
  - helmrelease.yaml
  # staging issuer is applied separately by a dedicated Kustomization (issuers)
  # production issuer is applied separately by a dedicated Kustomization (issuers)