apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: node-exporter-host-monitor
  namespace: monitoring
  labels:
    app: node-exporter-host-metrics
spec:
  selector:
    matchLabels:
      app: node-exporter-host-metrics
  endpoints:
    - port: metrics
      interval: 5s
      scrapeTimeout: 3s
      path: /metrics
