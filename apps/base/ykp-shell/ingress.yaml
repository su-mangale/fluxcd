apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ykp-ingress
  namespace: ykp-system
  annotations:
    # Keep interactive terminal WebSocket connections alive while idle.
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: ykp-api
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /docs
            pathType: Prefix
            backend:
              service:
                name: ykp-api
                port:
                  number: 8080
          - path: /openapi.yaml
            pathType: Prefix
            backend:
              service:
                name: ykp-api
                port:
                  number: 8080
          - path: /openapi.json
            pathType: Prefix
            backend:
              service:
                name: ykp-api
                port:
                  number: 8080
          - path: /healthz
            pathType: Prefix
            backend:
              service:
                name: ykp-api
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ykp-shell
                port:
                  number: 3000
